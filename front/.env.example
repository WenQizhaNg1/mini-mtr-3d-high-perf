# Leave empty to use the same-origin /api proxy (recommended).
VITE_TRAIN_API_URL=
# Optional development proxy target; production must reverse-proxy /api to Java.
API_PROXY_TARGET=http://127.0.0.1:3002
