# 项目约定

以 docs/architecture.md 和 docs/server-refactor-plan.md 为当前架构依据。

- front/：Vue 3 + MapLibre，浏览器交互、语言和视觉补间。
- server/：TypeScript + Hono，业务规则、PostGIS 查询、外部 API、缓存和调度。
- Martin：OSM 底图、线路及站点瓦片。
- data/、scripts/：源数据和 OSM 规范化构建。

前端仅通过 API 获取业务事实，不计算班次或重复实现实时匹配。数据库 schema 由版本化 migration 管理。保持 KISS，编辑器和视觉重做属于后续范围。旧根目录 src/、AMap/Three.js 链路已删除。
